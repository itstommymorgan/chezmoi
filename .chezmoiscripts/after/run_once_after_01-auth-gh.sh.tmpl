#!/usr/bin/env zsh

# run_once state is keyed by contents, so any edit here replays the script.
# Bail early rather than blocking on gh's "re-authenticate?" prompt.
if gh auth status >/dev/null 2>&1; then
  echo "gh is already authenticated."
  exit 0
fi

gh auth login
