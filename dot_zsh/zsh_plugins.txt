# Loaded after compinit -- the oh-my-zsh git plugin calls compdef at source time
# (10 times), and that function does not exist until compinit has run.

# zsh-vi-mode defines ~46 widgets and must come first. Anything that wraps
# widgets (autosuggestions, autopair, syntax-highlighting) has to wrap ZVM's
# final versions; if ZVM loads later it redefines already-wrapped widgets and
# the wrapper ends up calling itself -- "maximum nested function level reached".
jeffreytse/zsh-vi-mode

dracula/zsh path:dracula.zsh-theme
joshskidmore/zsh-fzf-history-search
ohmyzsh/ohmyzsh path:plugins/git
paulirish/git-open
MichaelAquilina/zsh-you-should-use
zsh-users/zsh-autosuggestions

# Both bind widgets over everything above, so they stay last.
hlissner/zsh-autopair
zsh-users/zsh-syntax-highlighting
