# Repo/project documentation for Claude Code -- belongs in the repo, never applied to ~.
# Patterns match TARGET paths, so the negation below is `.claude/`, not `dot_claude/`.
CLAUDE.md
**/CLAUDE.md
# ...except the user-scope one, which is genuine global Claude Code config.
!.claude/CLAUDE.md

# Root-level non-`dot_` files are applied to ~ by default, so anything meant to stay
# repo-only needs listing here. The Brewfile is read from the source dir by
# .chezmoiscripts/before/run_onchange_before_10-install-homebrew.sh (`--file="{{ .chezmoi.sourceDir }}/Brewfile"`),
# so a copy in ~ is never read -- it only goes stale and misleads a bare `brew bundle`.
MANUAL-SETUP.md
Brewfile
